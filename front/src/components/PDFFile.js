import React from "react";
import { Page, Text, Image, Document, StyleSheet } from "@react-pdf/renderer";

const PDFFile = ({ recomendations, indexes }) => {
  return (
    <Document>
      <Page key={1}>
        <Text>asdasd</Text>
      </Page>
    </Document>
  );
};

export default PDFFile;
